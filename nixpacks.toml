# Coolify / Nixpacks: run Node after build so /api/* (R2 upload, chat, visitors) works.
[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
