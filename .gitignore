node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
# PageIndex is a local-only dev tool (Python venv, never runs in the Node container).
# Kept out of git so Coolify's shallow submodule clone cannot break the deploy.
pageindex/
.claude/
dashboard-host/
DBExpo/
Guide_Completion/